<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ssm2.mapper.UserMapper">
    <select id="getAllUser" resultType="com.ssm2.bean.User">
        select * from user
    </select>

    <insert id="addUser" parameterType="com.ssm2.bean.User">
<!--        <selectKey resultType="java.lang.String" keyProperty="id" order="BEFORE">-->
<!--            select uuid();-->
<!--        </selectKey>-->

        insert into user (userName,gender) values (#{userName},#{gender})

    </insert>
</mapper>
